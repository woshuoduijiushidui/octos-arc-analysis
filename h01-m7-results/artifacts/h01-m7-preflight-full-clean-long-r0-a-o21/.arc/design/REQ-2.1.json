{
  "routes": [
    {
      "method": "GET",
      "path": "/api/notes",
      "request": {},
      "response": {
        "200": "{ \"notes\": [ { \"id\": \"string\", \"title\": \"Sprint goals\", \"content\": \"\", \"pinned\": true, \"archived\": false, \"deleted\": false, \"color\": \"\", \"labels\": [], \"createdAt\": \"string\", \"updatedAt\": \"string\" }, { \"id\": \"string\", \"title\": \"Groceries\", \"content\": \"\", \"pinned\": false, \"archived\": false, \"deleted\": false, \"color\": \"\", \"labels\": [], \"createdAt\": \"string\", \"updatedAt\": \"string\" } ] }"
      },
      "errors": []
    }
  ],
  "pages": [
    {
      "path": "/",
      "elements": [
        {
          "role": "heading",
          "name": "Pinned",
          "notes": "Section label for pinned notes. Must expose visible text matching /pinned/i (expectTextsVisible resolves getByText/getByRole heading). Only pinned notes (pinned===true, not deleted/archived) render under it."
        },
        {
          "role": "heading",
          "name": "Others",
          "notes": "Section label for regular notes. Must expose visible text matching /others|notes/i. Renders all non-pinned, non-deleted, non-archived notes."
        },
        {
          "role": "text",
          "name": "Sprint goals",
          "notes": "Pinned note title (FIXTURES.notes.pinnedTitle). Must be visible text; copied verbatim. Shown in the Pinned section."
        },
        {
          "role": "text",
          "name": "Groceries",
          "notes": "Regular note title (FIXTURES.notes.regularTitle). Must be visible text; copied verbatim. Shown in the Others section."
        }
      ]
    }
  ],
  "data_model": {
    "notes": {
      "id": "string",
      "title": "string",
      "content": "string",
      "pinned": "boolean",
      "archived": "boolean",
      "deleted": "boolean",
      "color": "string",
      "labels": "string[]",
      "createdAt": "string",
      "updatedAt": "string"
    },
    "labels": {
      "id": "string",
      "name": "string",
      "isDefault": "boolean"
    },
    "settings": {
      "view": "string",
      "enableBottom": "boolean"
    }
  },
  "files": [
    "backend/server.js",
    "backend/data/db.json",
    "frontend/src/App.js",
    "frontend/src/components/HomePage.js"
  ],
  "notes": "REQ-2.1 (fixture notes_overview) is read-only: openHome then expectTextsVisible('Sprint goals','Groceries') and expectTextsVisible(/pinned/i, /others|notes/i). Reuse the existing GET /api/notes route; the home page must fetch it on load and render two separated sections headed 'Pinned' and 'Others'. Seed data: for a NEW store (and the checked-in backend/data/db.json) initialize exactly the two required records — 'Sprint goals' (pinned:true) and 'Groceries' (pinned:false) — per UI_CONTRACT_DATA (initialize required records only for a new store; never overwrite existing user data). Both titles must be visible text on /; use actual heading elements for 'Pinned' and 'Others' so /pinned/i and /others/i match. Keep every REQ-1.1 element (Take a note..., Search, Main menu, Settings, List view) intact and the sidebar Notes label visible so openSidebar stays a no-op. Downstream nodes (REQ-2.2 create, REQ-2.8 pin) will render note cards in the notes-area; design the sections so note titles remain individually reachable by getByText/getByRole. Public tests are authoritative; report any conflict with the fixtures instead of silently dropping requirements."
}