# Octos Harness 优化与竞品调研清单

整理日期：2026-09-19。源码分析基于 `octos-arc` 提交 `c599d18c`。

优化范围仅为 Octos harness；官方需求文档、测试用例和判分条件作为固定输入。目标顺序为：**最终测试通过数与稳定性优先，其次减少全任务累计 token**。

以下收益是根据源码与作用机制给出的定性预期，尚未经过实现后的对照实验验证，不代表已证明的提升。部分策略会增加单次请求或修复成本，需要比较整个任务的总量。

## 优化清单

P0 表示优先验证正确性或补齐实验基础；P1 表示在基础可靠后优化执行效率；P2 表示按实际入口和任务需要开展。编号固定，后续调研和实验沿用。

| 编号 | 优先级 | 优化内容及当前依据 | 预计通过率收益 | 预计 token 收益与取舍 | 适用范围 | 竞品调研重点 |
| --- | --- | --- | --- | --- | --- | --- |
| H01 | P0 | **保留关键上下文证据**：当前默认摘要偏重首行与工具名，可能丢失需求约束、文件目标和具体失败。采用“不可变任务契约 + typed 证据胶囊 + 可选叙事摘要”，详见 [H01 竞品调研](./h01-context-evidence-competitor-research.md) 和 [H01 实施 Milestone](./h01-implementation-milestone.md)。 | 有望减少遗忘约束、重复失败修复；主要影响长任务。 | 确定性胶囊会增加少量固定输入，但有望减少重新调查和重复读取；模型摘要只作为后置可选项。 | 核心上下文；stdio 默认摘要；其他入口需核对实际压缩器。 | **待实现**：B 分支做 H01a–d；C 独立分支实验 H01e；H01f 不实现；最终跑 A/B/C 三臂对照。 |
| H02 | P0 | **文件缓存与模型可见内容一致**：stdio 重建任务 Agent、MCP 构造 Agent 时缓存接入不完整；缓存命中还需判断旧内容是否仍在上下文中。 | 有望避免模型依据已被压缩掉的代码修改文件，减少回归。 | 有望减少未变化文件的重复输入；上下文失效后仍须允许重新读取。 | stdio、MCP 的 Agent 构造及文件工具。 | 如何判断文件未变？如何处理压缩后的缓存失效、外部修改和跨任务隔离？ |
| H03 | P0 | **统一分页、截断与原文恢复**：文件页与上下文层可见预算不一致，可能发生二次截断；保留实际读取范围、续读参数和可恢复引用。 | 有望减少遗漏文件中间或末尾逻辑、误判日志和不完整重写。 | 有望减少盲目重读；过小分页会增加往返，须校准。 | stdio ContextManager、read_file；其他入口按实际预算核对。 | 文件和日志怎样分页？多层输出限制怎样配合？截断后如何定位并恢复原文？ |
| H04 | P0，条件性 | **补齐 MCP 任务压缩策略接线**：MCP 已挂 CompactionRunner，但正常 task loop 缺少聊天路径已有的部分压缩调用。 | 有望避免长任务溢出和声明保留策略未生效；主要影响 MCP 长任务。 | 有望限制输入增长；使用模型摘要时要计入摘要成本。 | MCP / Agent.run_task；只有该入口实际使用才优先。 | 不同入口是否共用压缩策略？何时触发？如何保护工具请求与结果配对？ |
| H05 | P1 | **已有代码优先局部编辑**：复用 edit_file、diff_edit、唯一匹配检查；改善定位失败反馈，减少无必要的整文件输出。 | 有望减少误删旧功能和修改范围过大造成的回归。 | 主要减少大文件输出；补丁匹配失败与重试可能抵消收益。 | 核心文件工具；原生 codegen 需要单独适配其输出协议。 | 整文件与补丁如何选择？怎样定位唯一位置、处理模糊匹配、反馈编辑失败？ |
| H06 | P1 | **验证失败后聚焦修复**：将合法任务的构建、交付格式或已提供验收错误反馈给当前任务，研究有预算限制的修复；MCP 当前部分验证失败直接返回 Failed。 | 有望提高可修复错误的完成率，减少外层整阶段重跑。 | 总量可能减少，也可能增加；必须统计新增通过数与额外修复 token。 | MCP 交付验证、核心任务执行；仅使用已提供或允许的检查。 | 验证发生在何时？失败是否在同一任务中修复？如何限制修复轮数并保留有效结果？ |
| H07 | P1 | **识别无效循环并切换策略**：复用现有循环检测和分类重试，结合错误观察、文件变化与操作判断进展，减少重复读写或执行。 | 有望让模型更早找到根因；误判停滞可能提前终止有效修复。 | 有望减少重复工具调用和无效模型请求；避免每一步增加付费反思。 | 核心 Agent loop；不同入口的收敛机制需核对。 | 怎样定义无进展？错误变化是否算进展？规则检测、反思与策略升级如何配合？ |
| H08 | P1 | **贯通并校准执行预算**：正确传递已有迭代、总 token、输出和推理配置；为修复和交付留额度，避免统一硬切。 | 有望减少预算提前耗尽、文件截断和来不及修复。 | 有望减少失控消耗及截断重发；更合理预算也可能允许任务消耗增加。 | 核心配置与任务入口；MCP 存在明确接线问题。 | 如何分配实现与修复预算？接近上限怎样收尾？推理与输出预算如何按阶段调整？ |
| H09 | P1 | **减少无关工具定义开销**：利用已有 ToolPolicy 和任务上下文收敛工具集合；保持必要能力和稳定序列化。 | 通过率收益待验证；删除必要工具可能降低完成率。 | 有望降低每次请求固定输入；频繁切换工具集合可能影响前缀缓存。 | 核心工具注册及模型请求，主要影响工具模式。 | 工具是全量、按阶段还是按需提供？怎样保证可发现、可调用和缓存前缀稳定？ |
| H10 | P0，实验基础 | **完整记录失败与重试消耗**：复用 partial usage / tracker，修正异常返回默认零消耗等口径；区分未知用量和真实零值。 | 不直接提高测试通过数；让其他策略的对比可信。 | 不直接降低实际 token；防止因漏算误判为节省。 | MCP 异常出口、核心用量与重试记录。 | 失败、超时、取消和丢弃响应怎样计量？如何与 provider 用量核对，防止漏算或重复算？ |
| H11 | P0，条件性 | **统一完整需求的呈现**：原生 compact 主要传 description，没有完整带入场景、依赖和设计背景；整理共享需求契约，官方文档原样保留。 | 有望减少漏需求及跨节点接口不一致。 | 首轮输入可能增加；完整信息若减少修复，整题总量可能下降。 | 原生 octos arc run；其他入口只处理实际收到的完整输入。 | 如何将需求、全局约束和依赖转换为模型输入？怎样去重而不损失语义？ |
| H12 | P0，条件性 | **补齐已有工程源码覆盖**：原生源码选择集合缺 .ts/.tsx/.jsx；先完整识别工程，再选择相关文件或片段。 | 有望减少在 TypeScript / React 模板上漏读实现和破坏已有结构。 | 提供必要源码会增加部分输入；有望减少错误生成与修复，净收益待测。 | 原生 octos arc run 的源码选择与预算计算。 | 如何识别语言与框架？用文件名、符号、依赖或语义选择源码？如何发现遗漏？ |
| H13 | P2，条件性 | **按所需能力选择生成档位和推理**：原生 Tiny / 推理强度主要按 spec 字符数决定；结合后端、持久化、鉴权和已有代码复杂度判断。 | 有望减少复杂任务误入静态页面档位，提高首轮成功率。 | 有望减少失败档位的白跑；简单任务误升档会增加成本。 | 原生 octos arc run；模型与参数选择需符合比赛允许范围。 | 如何判断任务复杂度？怎样选择直接生成、工具编辑和推理档位？失败后如何升级？ |

## 逐点调研的统一记录格式

对 H01—H13 分别建记录，先核实竞品源码或官方技术文档，再决定是否迁移。H01 已完成 Codex、Claude Code、DeepSeek Harness 的首轮调研；H02—H13 尚未开始，不把未经核实的策略填为结论。

| 记录项 | 要回答的问题 |
| --- | --- |
| 调研编号 | 对应 H01—H13 中哪一项？ |
| 竞品与版本 | 调研哪个 harness？对应哪个版本或源码 commit？ |
| 实现证据 | 官方文档或源码位置是什么？是否在默认执行路径上生效？ |
| 具体机制 | 输入是什么、何时触发、如何处理、输出是什么？ |
| 失败与边界 | 怎样处理信息缺失、编辑失败、压缩、重试及恢复？ |
| 效果证据 | 有无公开实验？模型、任务、预算和指标是否可比较？ |
| 迁移判断 | 哪部分适合 Octos？哪些假设不成立？是否符合只改 harness 的边界？ |
| 最小改动 | 复用哪些已有能力？需要修改哪些实际入口？ |
| 对照实验 | 官方输入固定时，比较什么数据？ |
| 决策 | 采用、调整后采用、暂缓或不采用，并给出理由。 |

## 实验口径与顺序

每项实验固定官方需求、测试集合、评分条件、模型、模板和资源；记录源码与实际二进制版本。先比较最终通过数与重复运行稳定性，再比较全任务累计输入/输出 token、真实请求数、修复次数和耗时。缓存与推理按对应 provider 和内核的计量契约汇总，避免重复相加。

调研可按编号逐点进行。实施前先确认实际入口并补齐 H10 的计量基础；当前 stdio 链路优先验证 H01—H03，再开展 H05—H09。H04、H11—H13 根据 MCP 或原生 ARC 入口是否实际使用调整优先级。

源码事实与更详细的适用条件见 [harness 优化分析](./harness-optimization-zh.md)。
